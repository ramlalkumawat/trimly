# Trimly — Salon Booking Frontend

This is a Vite + React + Tailwind CSS frontend scaffold for a production-quality salon booking UI (no backend). It uses dummy data and React Router for navigation.

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

This project includes a polished, responsive UI with components and pages ready for integration.
